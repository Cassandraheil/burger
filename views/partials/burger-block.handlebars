<li>
    {{burger_name}}
    <button class="change-devour" data-id="{{id}}" data-devoured="{{devoured}}">
        {{#if devoured}}Order Up{{else}}notate!{{/if}}
    </button>

    <button class="delete-burger" data-id="{{id}}">
        Devour!
    </button>
</li>